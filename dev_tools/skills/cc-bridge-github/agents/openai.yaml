interface:
  display_name: "CC_BRIDGE GitHub Release Maintainer"
  short_description: "Manage CC_BRIDGE dev, release, and GitHub state"
  default_prompt: "Use $cc-bridge-github to classify CC_BRIDGE changes, commit/push development work, wait for required workflows, and run dev/prepare/published checks until the GitHub-facing result is verified."
