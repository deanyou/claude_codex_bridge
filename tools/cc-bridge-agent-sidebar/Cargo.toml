[package]
name = "cc-bridge-agent-sidebar"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "CC_BRIDGE-native tmux sidebar that renders cc-bridge-daemon ProjectView"

[dependencies]
crossterm = "0.29"
ratatui = "0.30"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
lto = true
